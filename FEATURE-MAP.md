# 从代码引擎到skill的功能映射

此包目标是复现用户的“拖入文本→协商参数→确认开局→故事与选项”体验及核心机制，不是迁移网页应用所有基础设施。没有重新部署原Python服务/Vue界面，也不宣称全部运行行为逐行等价。

| 原功能 | 此skill实现 | 边界 |
| --- | --- | --- |
| 文本导入/章节索引/回合预算 | 标准库prepare + read-source | 无PDF/OCR，不吞解码错误；最大32MiB |
| 全文准备与人物/世界/锚点蒸馏 | 模型逐章读文、卡片、coverage与来源范围 | 程序只验证覆盖ID，不证明语义正确 |
| 基础/强化、难度、收束、正文档 | 配置schema与对话确认 | 两种模式都新增明确开局确认 |
| 人物/金手指设计 | 分阶段问答、完整规格与卡片 | 无数据库可视化设计器，模型负责语义质量 |
| 正文+A–F+自由行动 | 草稿→校验→原子commit | 六项结构硬验，文笔/动机/后果由模型判 |
| 涟漪、K、阵营、GF、动态收束 | 保留原模块+白名单计算器 | 数值计算可复核，证据输入和状态入账由skill负责 |
| 任务/奖励/正常碎锚 | references中的原公式与阶段账 | 不冒称已把原任务状态机全部接入硬结算 |
| 三愿码 | 原精确码，每次武装，最多3，成功登记才扣 | 世界事实语义仍需模型审查 |
| 永久码 | 原精确码、原确认词、不可撤销、全局解除锚点、多选增补 | 只本局，不授权真实系统/工具权限 |
| 规则问答/角色访谈 | 不推进回合的旁路 | 永久增补后规则查询改用“规则：”以免当增补 |
| 长记忆/每5幕近况/每10幕摘要 | 完整事件账+模型摘要 | 存档16MiB上限，不能无限增长 |
| 存档/续局 | 自动签名状态、checkpoint、同session续聊 | 不兼容原应用JSON；无任意导入或旧档回滚 |
| 小说导出 | 已提交正文Markdown+可选模型润色 | 润色副本不是存档 |
| 五领域桥段检索 | 原3409条+有限候选检索 | 不默认全库塞上下文；查询子串检索为此版适配 |
| 防提示词注入 | 数据隔离、路由、schema、保护字段、精确码、HMAC | 不是模型绝对安全证明；本机密钥可读，不能防本机文件控制者 |
| 模型供应商/缓存/并发/网页UI/局域网/Android | 不搬入skill | 使用当前宿主助手及工具，无额外模型API |
| 巨型分层语料/预设角色数据库 | 不打包 | 使用用户文本和用户确认角色；保留剧情桥段库 |

## 冲突决策

1. 人数用现行UI20伙伴/10伴侣/1宿敌，不用旧runtime各3。阵营数值原模块依然只取前三位。
2. 数值以原Python实现而非过时注释：round取偶、reversed最大步长.075、GF上界13；难度入口严格整数1–9，不保留原字符串解析“10→1”的错误兼容。
3. K是启发式文本相似度，不能替代因果验证。正常碎锚每幕最多一阶段，任务奖励按ID只发一次，不复制原接线可重复结算的缺陷。
4. 基础也可保存，但只续本局；不依从旧文案“基础完全不可存”而移除现行已有能力。
5. 不允许原文、角色文件或增补成为真实system prompt；“铁律”仅表示剧情事实优先级。
6. state.json签名、锁、revision和保护字段是本改编新增防护；不声称原引擎已经具备同样强度。

## 主要上游定位

相对于原包根目录：

- `core/engine/cheat_code.py:22–41,101–120,159–191`；`core/services/ask_service.py:128–232`：双码及固定效果。
- `core/engine/opening_flow.py`、`core/services/game_setup.py`、`frontend/src/App.vue`：配置/开局流程。
- `assets/prompts/options_gen.md`、`core/api/save_contract.py`：六项选择与提交格式。
- `core/engine/ripple.py,faction.py,golden_finger.py,dynamic_convergence.py`：数值核。
- `core/engine/quest.py,break_anchor.py`：任务与碎锚。
- `core/engine/tropes.py`、`assets/data/tropes_*.json`：桥段检索素材。
- `assets/rules/runtime.md,enhanced.md,state_memory.md`：语义与记忆规则，经现行代码校正。
