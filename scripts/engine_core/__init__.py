"""Reviewed NovelBorne 3.0.1 modules; no provider or runtime facade imports.

The six sibling modules are verbatim upstream copies. Import specific modules
explicitly; the original core.engine lazy export facade is deliberately omitted.
"""
